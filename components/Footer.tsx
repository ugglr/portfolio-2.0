import { ReactElement } from "react";

import styles from "../styles/Footer.module.css";

function Footer(): ReactElement {
  return (
    <footer className={styles.container}>carlw.se | Fullstack Developer</footer>
  );
}

export default Footer;
